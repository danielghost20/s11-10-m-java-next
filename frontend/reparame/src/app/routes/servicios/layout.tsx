import React from 'react'

const LayoutServicios = ({ children,
}: {
    children: React.ReactNode
}) => {
    return (
        <>
            {children}
        </>
    )
}

export default LayoutServicios